# Binaries
envsync
envsync.exe
/dist/

# Local EnvSync state (NEVER commit secrets or per-machine creds)
.env
.env.*
!.env.example
.envsync.json

# Go
/vendor/
*.test
*.out

# Design handoff prompts — never commit to the repo
claude_design_prompt.md
*_design_prompt.md
*design-prompt*.md
